/** @type {import('next').NextConfig} */
const nextConfig = {
  // NOTE: Below might be solution for using Cloudinary but could also be a force solution vs usesolution.
  // images: {
  //   loader: "cloudinary",
  //   path: "https://res.cloudinary.com/dingomalone/image/upload"
  // }
};

module.exports = nextConfig;
